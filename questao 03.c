#include<stdio.h>

void imprimir(int i,int n)
{
    for(i=1; i<=n; i++)
    {
        printf(" %d\n",i);
    }
}
int main ()
{

    int n,i;

    scanf("%d",&n);
    for (i=0; i<=n; i++){

    }

    imprimir(i,n);


    return 0;
}
